#!/usr/bin/env bash
# Script to display all active IPv4 IPs on the machine using ip command

# Use ip command to get IP addresses and filter the output
ip -4 -o addr show | awk '{print $4}' | cut -d/ -f1 > ~/output

# Inform the user where the output is saved
echo "IP addresses saved to ~/output"
